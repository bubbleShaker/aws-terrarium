# aws-terrarium — 計画

> テラリウム = ガラス容器の中の小さな生態系。
> クラウドを箱庭として外から観察し、負荷をかけて反応を見る。

## 何を作るか

AWS の主要 13 サービスを **ブラウザ内エミュレータ**として再現し、
**three.js のバーチャル空間**を歩き回りながら、負荷をかけて挙動を体感する学習アプリ。

GitHub Pages で静的配信する。実 AWS は不要（後述の「答え合わせモード」を除く）。

対象サービス: EC2 / ECS / Fargate / SQS / Aurora RDS / DynamoDB / SNS /
CloudFront / Lambda / EventBridge / EFS / S3 / Step Functions

## 設計判断の記録

### なぜ実 AWS ではなくエミュレータか

「過度なトラフィックで DynamoDB と RDS の違いを体感する」を **実 AWS でやると学習が成立しない**。

| | 実 AWS 実測 | 静的な図表 | **DES エミュレータ（採用）** |
|---|---|---|---|
| 費用 | 本気の負荷で数千円/回 | 0 | 0 |
| 再現性 | 無し。同じ負荷でも結果が揺れる | — | seed 固定で完全に決定論的 |
| 比較実験 | パラメータを変えた A/B が困難 | 不可 | ダイヤル 1 つで即比較 |
| 内部の可視化 | CloudWatch の集計値のみ。どのパーティションが詰まったかは見えない | — | パーティション単位で全部見える |
| 操作の体感 | 反映まで数分 | 無し | リアルタイム |

ホットパーティション・writer 単独制約・スロットリングは、いずれも**モデル化可能な決定論的現象**である。
実測より、パラメータを回して即座に結果が返るほうが学習効率が高い。

実 AWS 連携は捨てない。**M7 でオプトインの「答え合わせモード」**として足す
（自分の認証情報で小さな実測を走らせ、エミュレータの予測と並置する）。
最初から入れると認証とコスト管理が本体の進行を止めるため、後回しにする。

### なぜ tick ベースのハイブリッドか（純粋な DES ではなく）

リクエスト 1 個 = イベント 1 個の古典的な離散イベントシミュレーションは、
10 万 req/s を扱うと 1 秒のシミュレーションに 10 万イベントが必要で破綻する。

採用するのは **固定タイムステップ + 統計モデル + 粒子サンプリング**のハイブリッド:

- **時間**: 固定 tick（既定 100ms の仮想時間）で前進。3D の描画フレームと素直に対応する
- **数値**: 各 tick で「到着数をキー分布に従ってパーティションへ配分 → 容量チェック → 受理/スロットル/レイテンシ」を統計的に計算
- **表現**: 描画する粒子は全リクエストではなく**サンプリング**。統計値は正確なまま、60fps を保つ

seed 付き PRNG により、同じ設定なら常に同じ結果 → **単体テストできるシミュレータ**になる。

### 3 層アーキテクチャ

```
┌─ Content 層 ── シナリオ定義 (JSON/MD)「ホットパーティションを作れ」等
│                  └ 目標条件・初期パラメータ・解説文
├─ View 層 ────── three.js / React Three Fiber
│                  └ Core の状態を毎フレーム読んで描くだけ。ロジックを持たない
└─ Core 層 ────── 純粋 TypeScript のシミュレーションエンジン
                   └ three.js も React も import しない。Node 上で単体テスト可能
```

**依存の向きは Content → View → Core の一方向**。Core は上位層を知らない。
これにより Core だけを CLI やテストから駆動でき、3D の都合でモデルが歪むのを防ぐ。

## マイルストーン

| | 内容 | 状態 |
|---|---|---|
| **M0** | 調査・PLAN・リポジトリ整備 | ✅ 完了 |
| **M1** | Core engine の骨格 + DynamoDB パーティションモデル（3D なし・テストと CLI のみ） | ✅ 完了 |
| **M2** | three.js 空間に DynamoDB を投入。ダイヤルで負荷を上げ、1 本だけ赤熱するのを目撃する | ✅ 完了 |
| **M3** | Aurora writer モデル追加。**同じ負荷を両方に流して並置**する | ✅ 完了 |
| **M4** | 残りのサービスを順次追加（SQS / Lambda / S3 / SNS / EventBridge / Step Functions …） | 🚧 M4-1 (SQS Core) 完了 |
| **M5** | リソース間の関係性（IAM ポリシー・VPC・SG・アタッチメント）を空間の「配線」として表現 | |
| **M6** | シナリオ／チャレンジ機構、GitHub Pages 公開 | |
| **M7** | 実 AWS「答え合わせモード」（オプトイン） | |

## M1 の詳細スコープ

**ゴール**: 3D 抜きで、DynamoDB のホットパーティション現象を数値として再現し、テストで固定する。

実装するもの:

1. `Rng` — seed 付き PRNG（mulberry32）。決定論性の土台
2. `TokenBucket` — バーストキャパシティ（DynamoDB は未使用分を最大 300 秒ぶん保持する）
3. `KeyDistribution` — トラフィックがどのキーに向かうかのモデル
   - `uniform` — 理想的な設計
   - `zipf(s)` — 現実的な偏り。s を上げるとホットキーが出現する
   - `singleHot(ratio)` — 1 キーに集中。**分割不能な最悪ケース**
4. `DynamoDbTable` — パーティションモデル本体
   - パーティション数 = `max(ceil(RCU/3000), ceil(WCU/1000), ceil(sizeGB/10))`
   - **1 パーティションあたり 3,000 read units/s・1,000 write units/s**（公式上限）
   - アダプティブキャパシティの ON/OFF を切り替えられる ← 学習上の山場
5. `runScenario()` + CLI ハーネス — 結果を表として標準出力に出す

**M1 で体感させたい 1 行**:
> テーブル全体に 40,000 WCU を積んでも、1 つのパーティションキーに集中したら 1,000 WCU で頭打ちになる。
> アダプティブキャパシティは救ってくれるが、**単一キーは分割できない**ので救いきれない。

M1 に含めないもの: GSI、トランザクション、Streams、3D、Aurora。

### M1 の実装で新たに分かったこと（当初の計画に無かった収穫）

モデルを実際に動かしてみて、教材として当初想定より価値の高い現象が 3 つ見つかった。
いずれもシナリオとテストに組み込み済み。

1. **集計値はホットパーティションを隠す** — 50 本中 1 本が 42% スロットルしていても、
   テーブル全体で見ると 7.6% にしか見えない。CloudWatch の集計値だけ見ていて
   障害に気づけない事故の正体がこれ。→ サマリにパーティション単位の内訳を持たせた。
2. **バーストキャパシティが障害を数分間隠す** — 恒常的に容量超過していても、
   貯金が尽きるまでは完全に正常に見える。観測時間 60 秒では見逃す。→ シナリオを 900 秒にした。
3. **ハッシュ分散のむらは消せない** — キーを均等に設計しても、パーティションの負荷は
   均等にならない（理想 2.4% に対し最も重い本は 3.4%）。結果、40,000 WCU 積んで
   40,000 WCU 流すと 8.3% スロットルする。プロビジョニングに余裕が要る理由。

### M2 へ持ち越した既知の制限

- パーティション数の**履歴依存**（一度増えたら減らない）が未モデル化
- テーブル単位の**瞬間**上限が無い（保存則は成立している。下記参照）
- tick 履歴は既定 2,000 件へ間引く（15 分のシミュレーションで約 43MB）。
  ブラウザで動かす際は `maxRecordedTicks` をさらに絞る必要があるかもしれない

### バーストとプロビジョニング値についての注意

貯金を放出している間、**テーブル全体の受理量はプロビジョニング値を数分間超える**。
実測では 2,000 WCU のテーブルが 1.55 倍の 3,100 WCU を 400 秒間出す。
これはバグではなくバーストキャパシティの定義そのもの（暇な間に貯めた分を使っている）。

モデルが保証しているのは瞬間値ではなく**保存則**:

> 貯金ゼロから始めた場合、累計の受理量は「テーブルのキャパシティ × 経過時間」を決して超えない。

`test/capacityLane.test.ts` で検証している。無から容量を生み出していないことが健全性の根幹。

## M2 の入口（実装済み。View と Core の契約として残す）

以下は M2 の着手前に書いた設計メモだが、**実装後もそのまま契約として有効**なので残す。
実装の結果どうなったかは `summary/260801-m2-three-js-view.md` を参照。

### View 層が Core に対して知るべきこと

```ts
import { DynamoDbTable } from './core/services/dynamodb/table.js';
import { buildKeyWeights } from './core/services/dynamodb/keyDistribution.js';

const table = new DynamoDbTable({
  capacity: { mode: 'provisioned', readCapacityUnits: 1_000,
              writeCapacityUnits: 40_000, adaptiveCapacity: true },
  tableSizeGb: 10,
  itemSizeKb: 1,
  consistentRead: true,
  keyWeights: buildKeyWeights({ kind: 'singleHot', hotRatio: 0.9 }, 500),
});

table.partitionCount;     // 描くべき柱の本数
table.partitionWeights;   // 各柱が受け持つトラフィックの割合 (合計 1)

const tick = table.step({ readsPerSecond: 0, writesPerSecond: 30_000 }, 0.1);
tick.write.partitions[i]; // ↓ 柱 1 本を描くのに必要な値が全部入っている
//   demandedUnitsPerSec / acceptedUnitsPerSec / throttledUnitsPerSec
//   allowanceUnitsPerSec / burstTokens / burstDrawUnitsPerSec
//   utilizationVsBaseline  … 需要 / 頭割りの取り分
//   utilizationVsHardCap   … 需要 / 物理上限。1 に達したら打つ手なし ← 赤熱の指標はこれ
```

`runScenario()` は「シナリオを最後まで一気に流して集計する」バッチ用。
**M2 のインタラクティブな用途には使わない**（毎フレーム `table.step()` を呼ぶ）。

### M2 で必ず踏む設計判断 3 つ

**1. 描画フレームとシミュレーション tick を分離する**

ブラウザは 60fps（可変）、シミュレーションの tick は 0.1 秒の仮想時間で固定。
これを直結すると、フレームレートが揺れただけでシミュレーション結果が変わり、
M1 で苦労して確保した決定論性が壊れる。

固定タイムステップの蓄積方式を使う:
```
accumulator += 実フレーム経過秒 × 時間倍率
while (accumulator >= tickSeconds) { table.step(demand, tickSeconds); accumulator -= tickSeconds }
// 描画は最後の tick 結果を使う（補間は必要になってから）
```
「1 フレームで進める tick 数」に上限を設けること（タブ復帰時のスパイラルを防ぐ）。

**2. 粒子は全リクエストではなくサンプリングする**

30,000 req/s を粒子で描いたら即死する。**統計値は正確なまま、粒子だけ間引く**のが
M1 から一貫した方針（PLAN.md 冒頭「なぜ tick ベースのハイブリッドか」参照）。
粒子数は固定上限（数百程度）にし、各柱への配分を `partitionWeights` に比例させる。

**3. 赤熱の指標は `utilizationVsHardCap`**

`utilizationVsBaseline` ではない。物理上限との距離こそが「もう打つ手がない」を意味し、
M1 で一番伝えたい「単一キーは分割できない」に直結する。

### M2 のゴール（これが見えたら完了）

> ダイヤルで負荷を上げていくと、**1 本の柱だけが赤熱して粒子を弾き始める**。
> キー分布を `uniform` → `singleHot` に切り替えると、その瞬間に絵が変わる。

### M2 の実装で当初の想定と変わったこと

**アダプティブキャパシティを ON にしても、柱の色は変わらない。**

赤熱の指標は `utilizationVsHardCap`（需要 ÷ 物理上限）と決めたが、
アダプティブが変えるのは**通った量**であって需要ではない。つまり色は 1 ミリも動かない。

そこで柱を二重構造にした:

- 外側の半透明な柱 = **需要**（色 = 赤熱の指標）。アダプティブでは変わらない
- 内側の実体の柱 = **通った量**。アダプティブで伸びる。頭打ちのとき黄色
- 台座の色 = **バーストの貯金**。暗いほど残り少ない

「弾かれた量」は外側と内側の差として、そのまま形で見える。
赤は「需要が物理上限を越えている」ことだけに使う（内側まで赤くすると差が消えるため）。

## M3 の入口（新しいセッションはここから読む）

### 前提（M1 / M2 で確定済み・変えない）

- Core 層 (`src/core/`) は three.js も React も知らない。`test/architecture.test.ts` が機械的に守る
- Core は決定論的（seed 固定なら同じ結果）。フレームレートが揺れても結果が変わらないことは
  `test/simulationClock.test.ts` が固定している
- View は Core の状態を読んで描くだけ。ロジックを持たない

### View から Core を駆動する道具（M2 で用意済み・そのまま使える）

```ts
// src/view/App.tsx から見たパス
import { DynamoDbLiveSession } from '../core/scenario/dynamodb/liveSession.js';

const session = new DynamoDbLiveSession(settings); // 設定を渡してセッションを作る
session.advance(frameDeltaSeconds);          // 固定タイムステップで進む。返り値は刻んだ tick 数
session.latest;                              // 直近の tick 結果（毎フレーム読む用・生の数値）
session.snapshot();                          // View 向けに整形した状態（HUD 用・10Hz 程度で読む）
session.update({ writesPerSecond: 30_000 }); // 設定変更。テーブルの形が変わるなら作り直す
session.clock.timeScale = 20;                // 早送り
```

⚠️ **この API は M3 で変わった**（#9）。時計と負荷ダイヤルは `TerrariumDriver` へ移り、
セッションは `step(demand, tickSeconds)` だけを持つ被駆動体になった。下記「M3 で確定した駆動の形」を見ること。

Aurora を足すときは、**この `DynamoDbLiveSession` 相当を Aurora 用にもう 1 つ作る**のが素直。
`DynamoDbTable` と `AuroraWriter` は内部モデルが違いすぎるので、
無理に共通のインターフェースへ押し込めない（そこは M3 で判断する）。

### M3 の着手時にやると決めてあること（M2 のレビュー指摘の持ち越し）

- ✅ **`LiveSession` を `DynamoDbLiveSession` に改名する** — 完了（#9 / PR #11）。
  あわせて `LiveSettings` → `DynamoDbLiveSettings`、`SessionSnapshot` → `DynamoDbSessionSnapshot`。
  `PartitionView` / `LaneView` / `LaneKind` はすでに DynamoDB のドメイン概念を指す名前なので据え置いた
- ✅ **scenario 層を `dynamodb/` 名前空間へ移す** — 完了（#12）。下記「Core の構成」参照
- ✅ **`LaneKind` を `sim/demand.ts` へ移した**（#9）。実体は「`Demand` の 2 本のうちどちらか」で
  サービス非依存なので、`Demand` の隣が正しい置き場所だった。
  これで Aurora 側が read/write トグルを共有しても `dynamodb/` を import せずに済む
- ✅ **`DynamoDbSessionSnapshot` / `PartitionView` / `LaneView` は Core に残すことで確定**（#9）。
  3D 並置を実装して画面に出す数字が固まったので、保留していた再判断を済ませた。理由は 2 つ:

  1. `TerrariumDriver` が両サービスの snapshot を**同一時刻で 1 回に束ねて**返す。
     presenter を View へ出すと driver は生の tick 結果を返すことになり、
     「同じ瞬間の比較」を保証する場所が消える（並置の主張はそこが崩れると成立しない）
  2. Aurora 側の HUD を書いてみて分かったこと — **snapshot が持っているのは
     `activeSessions` (AAS) / `maxVcpu` (Max vCPU 線) / `connectionUtilization` のような
     AWS のドメイン語彙**であって、描画の都合ではない。
     ドメイン語彙が Core にあるのはクリーンアーキテクチャに反しない。
     View へ出すべきなのは「柱の高さ」「粒子の色」「席の数」のような描画の都合だけで、
     それらは既に `layout.ts` / `palette.ts` に分離してある

  → 「変更理由が違う」という M2 のレビュー指摘そのものは正しかったが、
  **違っていたのは分割線の位置**だった。線は snapshot の内と外ではなく、
  「AWS の語彙 / 画面の都合」の間にある。

### Core の構成（Aurora はここへ足す）

```
src/core/
├── sim/                  ← サービス非依存の土台。Aurora もそのまま使う
│   └── demand.ts  loadProfile.ts  rng.ts  simulationClock.ts
│       tokenBucket.ts  particleSampling.ts
│       ※ demand.ts の `Demand` は両サービスが受け取る共有の負荷型
├── services/
│   ├── dynamodb/         table.ts / partitioning.ts / keyDistribution.ts …
│   └── aurora/           ← M3 で新設。writer.ts（待ち行列モデル本体）
└── scenario/
    ├── driver.ts             ← M3 で新設。両サービスを束ねる TerrariumDriver
    ├── terrariumPresets.ts   ← M3 で新設。画面 1 枚ぶんの初期状態
    ├── dynamodb/             liveSession.ts / livePresets.ts / presets.ts / runScenario.ts
    └── aurora/               ← M3 で新設。liveSession.ts
```

`driver.ts` と `terrariumPresets.ts` が `scenario/` 直下にあるのは、
**両サービスを同時に import するファイル**だから。
どちらの名前空間にも属さないので、接頭辞も付けない
（`TerrariumDriver` / `TerrariumSnapshot` / `TerrariumPreset`）。

`TerrariumPreset` を `DynamoDbLivePreset` に Aurora の欄を足す形にしなかったのは、
名前が実態からずれるため（`LiveSession` で直したのと同じ罠）。
それに、この型が担う仕事は**両サービスを同時に元へ戻すこと**である。
インスタンスクラスを触ったあとにプリセットを押して Aurora だけ前の状態が残ると、
「同じ負荷を、容量の等しい 2 つへ流す」という看板シナリオが静かに崩れる。

### M3 で確定した駆動の形（#9・View はここだけを見る）

```ts
import { TerrariumDriver } from '../core/scenario/driver.js';

const driver = new TerrariumDriver({ load, dynamodb: ddbSettings, aurora: auroraSettings });

driver.advance(frameDeltaSeconds);       // 1 回で両方が同じ tick 数ぶん進む
driver.setLoad({ writesPerSecond: 500 }); // 共有の負荷ダイヤル。1 本しかない
driver.timeScale = 20;                    // 早送り。両方に等しく効く
driver.snapshot();                        // { simulatedSeconds, load, dynamodb, aurora } を同一時刻で
driver.dynamodb / driver.aurora;          // 個々のセッション（3D が latest を毎フレーム読む用）
```

セッション側は `step(demand, tickSeconds)` しか持たない。**時計も負荷も持たない。**

| 誰が持つか | もの |
|---|---|
| `TerrariumDriver` | `SimulationClock`（1 本）/ 負荷ダイヤル（1 本） |
| `DynamoDbLiveSession` | テーブルの形、作り直しの判断、DynamoDB 用 presenter |
| `AuroraLiveSession` | writer の諸元、作り直しの判断、Aurora 用 presenter |

共通インターフェースへは押し込めない（PLAN.md の当初の見立てどおり）。
**共有するのは時間と負荷だけでよく、それ以外に共通項が無い。**

テストで固定していること（`test/terrariumDriver.test.ts`）:

- フレーム時間が揺れても、`timeScale` を途中で変えても、両者の経過時間が 1 tick もズレない
- 同じ 500 q/s で**受理 400 / 拒否 100 が両者一致し、Aurora だけ 2,651ms**（M3 の看板）
- ρ=1.05 では**エラー 0 のままレイテンシだけが秒へ伸びる**区間が存在する
- 同じ設定・同じフレーム列なら 2 回走らせて完全に同一（決定論性）
- `'clock' in driver.dynamodb === false`（View から個々の時計に手が届かない構造テスト）

テストは `test/` にフラットに置き、**対象の識別子名に合わせる**
（`dynamoDbLiveSession.test.ts` / `dynamoDbScenario.test.ts`）。
`test/` 側もディレクトリで名前空間を切るかは、Aurora のテストが増えてから判断する。

**ディレクトリが名前空間を担う**ので、ファイル名に接頭辞は付けない
（`scenario/dynamodb/liveSession.ts`。`dynamoDbLiveSession.ts` ではない）。

一方、**export する識別子には接頭辞を付ける** — View 層は 1 画面に両サービスを並べるため、
同じファイルが両者を import する。`services/dynamodb/table.ts` が `DynamoDbTable` を
export しているのと同じ慣習に揃える。

| 接頭辞を付ける | 付けない |
|---|---|
| `DynamoDbLiveSession` / `DynamoDbLiveSettings` / `DynamoDbSessionSnapshot` | `PartitionView` / `LaneView` / `LaneInfo` |
| `DynamoDbLivePreset` / `dynamoDbLivePresets` / `defaultDynamoDbLivePreset` / `findDynamoDbLivePreset` | **`runScenario.ts` / `presets.ts` が export するものはすべて**（`Scenario` / `ScenarioResult` / `PartitionSummary` / `presets` / `findPreset` / 各シナリオ定数 …） |

線引きの基準は「**1 つのファイルが両サービスを同時に import するか**」。
バッチ経路（`runScenario.ts` / `presets.ts`）は CLI 専用でその状況が来ないので素のままでよい。

**バッチ経路が衝突したらどうするか**（M3 で並置ドライバを CLI 側にも作る場合）:
接頭辞は付けず、**名前空間 import で受ける**。

```ts
import * as dynamodb from './scenario/dynamodb/runScenario.js';
import * as aurora   from './scenario/aurora/runScenario.js';
```

17 個の export に接頭辞を付けて回るより安い。この判断を先に決めておくのは、
M3 で同じ議論が再燃するのを防ぐため。

⚠️ ただし `PartitionSummary` だけは別の問題を抱えている。**Aurora にパーティションは存在しない**ので、
これは import 衝突とは無関係に「汎用の名前で DynamoDB 固有の形を指している」— `LiveSession` で直したのと
同じ罠が残っている。バッチ経路に手を入れるときに一緒に直す。

### M3 で気をつけること

- **同じ負荷を両方に流して並置する**のが M3 の本体。
  片方ずつ見せても対比は生まれない。1 画面に 2 つ置く前提で空間を設計する
- 粒子の表現が両者の差になる:
  DynamoDB = 頂上で赤く散る / Aurora = writer の手前で**詰まって列を成す**
- Aurora 側は「待ち行列 → レイテンシ」のモデルが要る。M2 までのモデルには時間軸の
  待ち行列が存在しないので、Core に新しい概念が入る最初のケースになる

### M3 の待ち行列モデル（調査で確定・実装の前提）

詳細は `research/260801-aurora-queueing-model.md`。着手前に読むこと。Issue #9 に対応。

**壁は 2 枚あり、性質が違う**:

| | 壁A: vCPU 本数 | 壁B: max_connections |
|---|---|---|
| 挙動 | **拒否せず待たせる**（M/M/c の窓口数 c） | **拒否する**（`Too many connections`） |
| db.r6g.large での値 | **2** | **1,000** |

> **並ぶ場所（1,000）が、捌ける本数（2）の 500 倍広い。**
> だから Aurora は「エラーを出さずに、ひたすら遅くなる」ことができる。
> 接続数の上限は性能の上限ではなく、**待合室の広さ**でしかない。

**レイテンシは 2 項の和にする**（最重要の設計判断）:

```
W = Q / totalRate      背圧項: いま並んでいる列が掃ける時間（過負荷と過渡状態を担当）
  + Wq(c, μ, λ)        統計項: Erlang C（飽和以前のゆらぎを担当。ρ は 0.99 でクランプ必須）
```

流体モデル単独では **ρ=0.98 でも待ち時間が厳密に 0** になり、
「飽和する前から遅くなる」というキャパシティ設計で最も重要な現象が丸ごと消える
（利用率 50% → 95% で、使う容量は 1.9 倍なのに待ち時間は **28 倍**になるのが本来の姿）。

閉形式（Erlang C）は**エンジンではなくテストオラクル**に使う。
ρ<1 でだけ理論値に照合し、ρ≥1 は M1 と同じ**保存則**で守る。
この経路に乱数は 1 つも無いので、DynamoDB 側より**さらに強く決定論的**になる。

**HUD は Performance Insights と同じ読み方にする**（独自の指標名を発明しない）:

| 待ち行列理論 | Performance Insights |
|---|---|
| L（系内客数） | **AAS / DBLoad** |
| c（窓口数） | **Max vCPU 線** |
| ρ = L/c | AAS ÷ Max vCPU（1 を超えたら待ち行列） |

学習者が本物の Performance Insights を初めて開いたとき、そのまま読めることを狙う。

**「自力で戻れない」には増幅が要る** ⚠️ 当初の想定と違った点:

素の待ち行列は**ちゃんと自力で戻る**。安全な負荷 → スパイク → 安全な負荷へ戻すと、
再送が無ければ行列は 30 秒で掃ける（Q: 960 → 0）。戻れなくなるのは**再送が加わったときだけ**
（Q: 960 → 960 のまま張り付く）。これは metastable failure として知られる現象で、
**トリガーではなく持続フィードバックループが本体**である。

→ 「行列が伸びること」と「戻れなくなること」は**別の現象**。分けて教えたほうが正確で学びが深い。
→ 「トリガーを取り除いても劣化が続く」はそのまま決定論的テストになる。M1 の保存則テストに相当する看板テスト。
→ 実装の罠: 再送を「行列長に比例」と書くと **1e151 まで発散する**（調査中に実際に踏んだ）。
  再送は**有限のクライアント母集団で閉じる**こと。

**M3 のスコープ外**（理由つき）: USL による容量劣化（α・β に公式の根拠が無く校正できない。
推測値でモデルを複雑にすると教材の信頼性が落ちる）/ RDS Proxy の 3 段目のキュー /
Kingman のばらつきダイヤル / reader・レプリカラグ / Aurora Serverless v2。

### M3 の空間設計（決定済み・実装前に合意した）

**1. 1 つの three.js 空間に 2 つ置く。** 左右分割の別シーンにはしない。

```
┌─ 1 つの空間（自由に歩ける）───────────┐
│            [負荷ダイヤル]              │
│                 │                      │
│          ┌──────┴──────┐               │
│          ▼             ▼               │
│    ▓▓ ██ ▓▓ ▓▓      ╔═════╗            │
│    ▓▓ ▓▓ ▓▓ ▓▓      ║  ⚙  ║ writer     │
│      ✦  ✦           ╚═════╝            │
│    弾かれて散る      ●●●●●●● ← 列      │
└────────────────────────────────────────┘
```

決め手は**負荷の源を 1 本のパイプとして実在させられる**こと。
調査で「受理も拒否も同じ数字で、レイテンシだけが違う」と分かったので、
「同じ負荷を流している」が HUD 上の約束事ではなく**目に見える形**である必要がある。
「歩き回る箱庭」というプロジェクトの前提とも合う。

厳密な数値比較がアングル依存になるのが弱点。必要になったら
「1 つの空間 + 2 窓の比較ビュー」へ育てる（最初から作らない）。

**2. 負荷ダイヤルは 1 本の共有。** 切り離しトグルも付けない。

M3 の主題は「同じ負荷を流したときの差」なので、ダイヤルが 1 本なら**実験が必ず公平になる**。
ずらせないことが教材としての正しさを保証する。

⚠️ **実装上の帰結**: 2 つのセッションが**同じ仮想時間で進む**ことを保証する必要がある。
`DynamoDbLiveSession` は `SimulationClock` を内部に持つので、Aurora 用にもう 1 つ作ると
時計が 2 つになる。同じ `advance(delta)` を配れば結果は一致するが、
`timeScale`（早送り）を片方にだけ適用すると静かにズレる。
→ **両方を束ねる driver をひとつ作り、時計の操作はそこだけが持つ**。
View が個々のセッションの `clock` を直接触らない形にする。

✅ **完了（#9）**。下記「M3 で確定した駆動の形」を参照。
セッションから時計を**取り上げた**（残したまま driver を被せると、使われない時計が
`simulatedSeconds` に 0 を返し続ける罠になる）。負荷も同じ理由で
`DynamoDbLiveSettings` から外し、driver が 1 本だけ持つ形にした
（設定に残すと、片方だけ違う負荷を流せる経路が残ってしまう）。

**3. Aurora のインスタンスクラスをダイヤルにする。**

| クラス | vCPU | max_connections |
|---|---|---|
| db.r6g.large | 2 | 1,000 |
| db.r6g.xlarge | 4 | 2,000 |
| db.r6g.2xlarge | 8 | 3,000 |
| db.r6g.4xlarge | 16 | 4,000 |

PLAN.md の対比「水平・自動 vs **垂直・手動**」が、操作として完成する。
そして上げていくと**窓口は 2→4→8 としか増えないのに、待合室は 1,000→2,000→3,000 と増える**のが見える。
§2-4 の「並ぶ場所が捌ける本数の 500 倍広い」が、ダイヤルを回すだけで体感できる。

✅ **決定（#9）: クラスを変えたら待ち行列はリセットする。**
実機の Aurora はインスタンスのスケールに**フェイルオーバー（数十秒の断）を伴う**ので、
溜まった接続が新しいインスタンスへ引き継がれることはない。教材としても
**「障害の最中にスケールして逃げ切ることはできない」**が伝わる。
引き換えの「行列が掃けていく様子」は**負荷ダイヤルを下げれば見られる**（そちらは作り直さない）ので失われていない。

⚠️ ただし **再送ポリシーの変更ではリセットしない**（これも #9 で決めた）。
再送はクライアント側の振る舞いであって、サーバに並んでいる客は消えないため。
ここでリセットすると、メタステーブル障害からの回復理由が
「増幅を断ち切ったから」ではなく「リセットしたから」に見えてしまい、
M3 の看板そのものが壊れる。そのため `AuroraWriter` には再送だけを差し替える setter がある。

### M3 の 3D 並置で確定したこと（#9・実装しないと分からなかった 4 つ）

**1. 粒子の縮尺は「レート」と「ストック」で 2 系統に分ける。**

| | 何を表すか | 縮尺 | 両サービス共通か |
|---|---|---|---|
| 粒子 | 到着レート (件/秒) | `requestsPerParticle(共有負荷)` | **共通** |
| 席 | 待ち行列長 Q (件) | 1 席 = 10 件 | Aurora だけ |

粒子の縮尺は**共有の負荷ダイヤル 1 本から導く**。
サービスごとに持たせて「揃えるのを忘れない」と決意するより、
1 本から導いて**一致を構造で保証する**ほうが確実である（時計を 1 本にしたのと同じ理由）。
`test/viewLayout.test.ts` が「同じ負荷なら両者の粒子数が一致する」を固定している。

固定値にしなかったのは、この画面が扱う負荷が 400 q/s（Aurora の壁）から
60,000 q/s（DynamoDB の壁）まで**150 倍の幅**を持つため。1-2-5 の梯子へ量子化してある。

⚠️ 席と粒子を無理に同じ縮尺へ乗せてはいけない。単位が違う (件/秒 vs 件) ので、
共通の縮尺で 1,000 件を描くと席が 7 個になり「待合室 1,000 席」が消える。
両者は **Little の法則の両辺** — 席が L（何人並んでいるか）、
粒子が待合室を横断する時間が W（1 人がどれだけ待つか）— を分担している。
だから「席は空いていないのに床は赤い」（＝飽和前のゆらぎで既に遅い）が絵として出る。

**2. レイテンシは「粒子が待合室を横断する速さ」で見せる。**

数字を読まなくても、Aurora の粒子が這っている間に DynamoDB の粒子が
何往復もしているのが見えれば、それが「レイテンシだけ桁違い」の中身である。
弾かれる場所も対比になっている — DynamoDB は**柱の頂上**で即座に、
Aurora は**待合室の入口**で、しかも席が埋まりきってから。

**3. 負荷ダイヤルの目盛りは有効数字 2 桁の値を並べたものにする。**

線形では足りない。0〜60,000 を 120 目盛りに切ると 1 目盛りが 500 req/s になり、
**Aurora の帯域全体が 1 目盛りに潰れる**。完了条件の
「エラーが 1 件も出ないままレイテンシが伸び続ける区間」(ρ=1.05) にダイヤルが届かない。

⚠️ 最初は指数関数の値を読みやすく丸める形で書いて 2 つ壊した。
低い側で隣の目盛りが同じ値になり、丸めた結果が 490 / 512 になってプリセットの 500 に届かなかった
（プリセットを読み込んだあとスライダーを掴んだ瞬間に負荷が飛ぶ）。
梯子そのものを目盛りにすれば、キリのよい値には必ず手が届く。

**4. カメラ距離は目分量ではなく要件から逆算する。**

満たすべき条件が 2 つあり、厳しいほうが距離を決める:

- **横**: 2 つの敷地が、幅 330px のパネルに隠れない帯 (`VISIBLE_WIDTH_FRACTION`) の中へ収まること
- **縦**: 地面から分岐のパイプ (`PIPE_TOP`) までが収まること

係数を手で決めていたときは、パーティション数や `max_connections` を変えた瞬間に
どちらかが破綻した（実際、待合室がコントロールパネルの下に潜った）。
`test/viewLayout.test.ts` が両方を固定している。

⚠️ もう 1 つ踏んだ罠: 上限面のサイズにカメラ距離用の `gridExtent` を使うと、
パーティション 1 本のテーブルで下限 6 が効いて**面だけが敷地の 5 倍に広がり**、
隣の Aurora の敷地へめり込んだ。寸法は `gridWidth`（実寸）から出すこと。

## M3 で対比させる核心

エミュレータの価値は「並べて初めて分かる」ことにある。

| | DynamoDB | Aurora（writer 単独） |
|---|---|---|
| スケールの単位 | パーティション（水平・自動） | インスタンスクラス（垂直・手動） |
| 限界の壁 | パーティションあたり 3,000 RCU / 1,000 WCU | **vCPU 本数**（＝同時に走れるクエリ数） |
| **限界を超えたときの壊れ方** | **即座に拒否する**（`ProvisionedThroughputExceededException`） | **さんざん待たせてから拒否する**（CPU 待ち行列 → レイテンシ爆発 → やがて `Too many connections`） |
| 復旧の仕方 | リトライ（指数バックオフ）で吸収できる | 行列が伸びるだけなら自力で戻る。**再送が加わると戻れなくなる** |
| 読み取りのスケール | パーティション追加で線形 | reader 追加（ただしレプリカラグが出る） |

⚠️ **かつてここに「拒否 vs 待ち行列」と書いていたが、二分法としては不正確**だった
（`research/260801-aurora-queueing-model.md` の調査で判明）。Aurora も max_connections で拒否する。
差は「拒否するか否か」ではなく **どれだけ待たせてから拒否するか**にある。
そして**遅れて拒否するほうが厄介**である — 気づいたときには待合室が埋まっている。

**この「即座に拒否 vs 待たせてから拒否」の非対称性が、両者の設計思想の差そのもの**であり、
本プロジェクト全体で一番伝えたい 1 点。3D 表現もこの差が視覚的に出るように設計する
（DynamoDB = 弾かれた粒子が赤く散る / Aurora = 粒子が writer の手前で詰まって列を成す）。

### 数字で言うとこうなる（M3 の調査で実測）

同じ 500 q/s を、どちらも容量 400 q/s 相当のサービスへ流したとき:

| | 受理 | 拒否 | レイテンシ |
|---|---|---|---|
| DynamoDB | 400 q/s | 100 q/s | **5 ms**（一定） |
| Aurora | 400 q/s | 100 q/s | **2,646 ms** |

> **スループットの数字は完全に同一。違いはレイテンシにしか出ない。**
> CloudWatch でスループットとエラー率だけを並べたら、この 2 つは見分けがつかない。

「並べないと分からない」という本プロジェクトの前提が、そのまま数値で裏付けられた。

## M4 の入口（新しいセッションはここから読む）

### 刻み方

M4 は 6 サービス以上を並べた粗いマイルストーンなので、**1 サービスずつ・Core → 3D の 2 段階**で進める。
M2（DynamoDB）/ M3（Aurora）と同じ粒度である。

| | 内容 | 状態 |
|---|---|---|
| **M4-1** | SQS のバックログモデルを Core に追加（3D なし） | ✅ 完了（#20） |
| **M4-2** | SQS を 3 つ目の敷地として three.js 空間へ置く | 次はここ |
| **M4-3 以降** | Lambda（同時実行数・コールドスタート）→ S3 → SNS / EventBridge … | |

### なぜ SQS を最初に足したのか

過負荷時の壊れ方に **第 3 の類型「拒否も遅延もせず、ただ溜める」** を足すと、三分類が完成する。

| | 過負荷になると | 壁 | 検知できる指標 |
|---|---|---|---|
| DynamoDB | 即座に拒否 | パーティション容量 | エラー率 |
| Aurora | 待たせてから拒否 | vCPU → max_connections | レイテンシ |
| **SQS** | **溜める。何も起きない** | **保持期間 / in-flight** | **`ApproximateAgeOfOldestMessage`** |

### SQS モデルの前提（AWS 公式・裏取り済み）

- Standard queue の**スループットは実質無制限**、積めるメッセージ数も**無制限**
  → **入口に壁が無い**。`SqsQueue.step()` の受付側に `Math.min` が 1 つも無いことがモデルの主張そのもの
- 同時実行の壁は **in-flight 約 120,000 件**（受信済み・未削除）。
  ⚠️ これは**積める件数の上限ではない**。混同が「SQS にも容量制限がある」という誤解の出どころ
- **保持期間 既定 4 日 / 最小 60 秒 / 最大 14 日**。超えたメッセージは**黙って消える**

出典は `src/core/services/sqs/limits.ts` に URL 付きで書いてある。

### M4-1 の実装で分かったこと（当初の想定と違った 3 つ）

**1. 年齢を Little の法則で近似すると、看板が消えるどころか警報が逆を向く。**

`年齢 ≈ Q ÷ 消化レート` は定常状態では正しいが、過渡状態で**動く向きが逆になる**。
過負荷を止めて負荷を安全域へ下げたとき:

| | 過負荷のピーク | 20 秒後（安全域で回復中） |
|---|---|---|
| 実際の最古メッセージの年齢 | 20.0 秒 | **24.0 秒（伸びている）** |
| `Q ÷ 消化レート` の近似 | 25.0 秒 | **20.0 秒（減っている）** |

先頭のメッセージは負荷が正常に戻っても**依然として先頭のまま**なので、
掃けきるまで 1 秒につき 1 秒ずつ齢を重ねる。近似だと「もう大丈夫」に見える。
M3 で踏んだ罠と同型（流体モデル単独では ρ=0.98 でも待ち時間が厳密に 0 になる）。

→ **累積到着量 A(t) を区分線形の折れ線で持ち、FIFO で厳密に逆引きする**（`arrivalLedger.ts`）。
節を増やすのは**到着レートが変わった瞬間だけ**なので、負荷ダイヤルを人が回す限り
tick を何万回刻んでも節は数個しか増えない（`test/sqsArrivalLedger.test.ts` が固定）。

**2. ⚠️ キューの深さの上限は「到着レート × 保持期間」— 消化レートは 1 ミリも効かない。**

着手前は「消化レート × 保持期間」だと書いていたが、実装して回したら違った。
キューに残っているのは**保持期間ぶんの到着量**そのものである
（それより古いものは捌けたか消えたかのどちらかで、もう居ない）。

到着 500 件/秒・保持 60 秒なら、consumer が 2 個でも 200 個でも上限は 30,000 件。
違うのは**そこへ到達するかどうか**だけ。

同じ理由で、待ち時間を `Q ÷ 消化容量` と書いてはいけない。
前に並んでいる客は**期限切れでも減る**ので、30,000 ÷ 400 = 75 秒という
「60 秒で消える列に 75 秒並ぶ」ありえない値が出る。出ていく総レートで割ること。

**3. 設定変更でリセットするかどうかが、Aurora と正反対になった。**

| | スケール操作 | 溜まったもの |
|---|---|---|
| Aurora | インスタンスクラス変更 | **リセットされる**（実機がフェイルオーバーを伴う） |
| SQS | consumer 数変更 | **持ち越す**（キューの外側の話なので何も起きない） |

> Aurora は障害の最中にスケールしても逃げ切れない。
> SQS は溜まったバックログを consumer を増やして掃ける。**それが正しい手**である。

ここでリセットすると、キューの唯一にして最大の利点 —「溜めておいて後から捌ける」— が
「溜めたものが無かったことになる」に化ける。
→ `SqsLiveSession.update()` は**決して作り直さない**。作り直すのは `replace()`（プリセット）だけ。

### M4-1 で確定した Core の構成

```
src/core/
├── services/
│   ├── dynamodb/  ├── aurora/
│   └── sqs/           ← M4-1 で新設
│       ├── limits.ts         公式値と仮定値（URL 付き）
│       ├── arrivalLedger.ts  累積到着量の折れ線。年齢を FIFO で厳密に逆引きする
│       └── queue.ts          SqsQueue（バックログモデル本体）
└── scenario/
    ├── driver.ts             ← 3 サービスを同一時刻で束ねる
    └── sqs/liveSession.ts    ← M4-1 で新設
```

`TerrariumDriver` は**サービスを足すたびにここだけが太る**。それは意図した設計で、
増えるのは「同じ負荷を配る」「同じ瞬間で切り出す」の 1 行ずつだけ。
各サービスのモデルは互いを一切知らないまま独立していられる。

⚠️ プリセット `same-load-both` は **`same-load-all` へ改名した**（3 つ目が加わって「both」が
実態からずれたため）。識別子も `sameLoadBothSides` → `sameLoadEverywhere`。
このプロジェクトが繰り返し直してきた罠（`LiveSession` → `DynamoDbLiveSession`）と同じもの。

### M4-2（次にやること）で決めること

- **3 つ目の敷地をどこに置くか。** M3 の空間は 2 つの敷地を左右に並べ、
  カメラ距離を `test/viewLayout.test.ts` が「パネルに隠れない / パイプが画角から抜けない」で
  固定している。3 つ目を足すとこの逆算が効かなくなる可能性が高い。**先にテストを見ること**
- **粒子の縮尺は共有のまま。** `requestsPerParticle(共有負荷)` から導く方式を崩さない
  （「同じ負荷なら粒子数が一致する」が構造で保証されている理由がこれ）
- **SQS 固有の表現**: 弾かれる粒子が無いので、DynamoDB / Aurora と絵で差が付く。
  伸び続ける列そのものと、**先頭で黙って消える**瞬間をどう見せるかが山場
- HUD は `ComparisonHud` に 3 列目を足す。指標名は CloudWatch のまま
  （`ApproximateAgeOfOldestMessage` を刷り込むのが狙い）

### M4-1 のスコープ外（理由つき）

- **FIFO キュー** — TPS 上限という別の壁が入り、Standard の「入口に壁が無い」が濁る
- **DLQ / 可視性タイムアウト超過による二重処理** — Aurora の再送増幅と同型で価値は高いが、
  まず「溜まるだけ」の基本形を通す
- **ロングポーリング / バッチ受信** — API 呼び出し回数の話であって、バックログの話ではない

## 技術選定

- **Vite + React + TypeScript** — GitHub Pages に静的ビルドを吐ける
- **three.js + React Three Fiber + drei** — `photo-orb` で実績のある構成を流用する
- **vitest** — Core 層の単体テスト
- **GitHub Actions** — `main` push で Pages へデプロイ

## 用語メモ（このプロジェクト内の定義）

- **tick** — シミュレーションの 1 ステップ。既定 100ms の仮想時間
- **Core** — 純粋 TS のシミュレーション層。UI を一切知らない
- **シナリオ** — 初期パラメータ + 目標条件 + 解説文のセット
